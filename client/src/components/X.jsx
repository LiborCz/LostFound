import React from "react";

  // const hWScroll = async (e) => {
    
  //   e = (e.target === document) ? document.documentElement : e.target;

  //   const isBottom = e.scrollHeight - e.scrollTop === e.clientHeight;

  //     if(isBottom && items.length) {
        
  //       let itemsLeft = total - items.length;

  //       if(items.length>0 && itemsLeft>0) {
  //         console.log(`Jeste jich je:${itemsLeft} (length: ${items.length})`);
  //         // console.log(items);

  //         if(itemsLeft!==lastLeft) {
  //           setSkip(items.length);
  //           lastLeft = itemsLeft;
  //         }
  //       }
  //     }
  // }

  function hWResize() {
  }

  // window.addEventListener('scroll', hWScroll);
  // window.addEventListener('resize', hWResize);
